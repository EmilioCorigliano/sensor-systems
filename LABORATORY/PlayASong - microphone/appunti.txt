priority group: set in preemption priority in System Core>NVIC (set also the priority group bits)

__HAL_GPIO_EXTI_CLEAR_IT(GPIO_PIN_8) to clear interrupts generated by the microphone

clear interrupt of timer overflow after initialization (writing a zero in the timer value generates an interrupt)